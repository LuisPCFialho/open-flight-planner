node_modules/
dist/
.npm-cache/
*.local
# `*.local` ja apanha o `.env.local`. Este e para o `.env` sem sufixo, que e
# onde uma chave a serio acaba por ir parar quando se tem pressa.
.env
.DS_Store
sqlite_mcp_server.db
iniciar.log
preview-teste.log
coverage
test-results
playwright-report

# Acrescentado pelo `vercel link`.
.vercel
.env*

# Menos o exemplo, que e para ser seguido. Ja esta no indice e por isso nao
# desaparecia agora - mas quem o apagasse e voltasse a criar nao o conseguia
# acrescentar outra vez, e isso da uma tarde perdida a perceber porque.
!.env.example
