## What this changes

<!-- What the reader sees differently after this, in a sentence or two. -->

## Why

<!-- If it fixes a defect, say what the defect did, not just its name. The
     comments in this codebase record defects that were paid for once; a pull
     request description is where the next one gets written down. -->

## Checks

- [ ] `npm run build` passes (lint, types, bundle, and the `dist` check)
- [ ] `npm test` passes, and new behaviour has a test that would fail without it
- [ ] Comments and interface text in European Portuguese
- [ ] Nothing was invented: any unconfirmed value is marked `porConfirmar`

<!-- If a test failed and you changed it, say what made the old expectation
     wrong. Several of them describe defects that took a morning to find. -->
